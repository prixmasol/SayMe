<div class="hero-unit">
  <form id="zonaProfile-form" class="form-horizontal" action="/zona_profiles" method="POST">
    <fieldset>
      <legend>Create a new ZonaProfile</legend>
      <% if(params.errors) { %>
      <div class="control-group">
        <ul>
        <% for(var err in params.errors) { %>
          <li><%= params.errors[err]; %></li>
        <% } %>
        </ul>
      </div>
      <% } %>

      <%- partial('form', {zonaProfile: params, dataR: dataR, dataC: dataC}) %>

      <div class="form-actions">
        <%- contentTag('input', 'Add', {type: 'submit', class: 'btn btn-primary'}) %>
      </div>
    </fieldset>
  </form>
</div>
