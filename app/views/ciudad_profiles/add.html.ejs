<div class="hero-unit">
  <form id="ciudadProfile-form" class="form-horizontal" action="/ciudad_profiles" method="POST">
    <fieldset>
      <legend>Create a new CiudadProfile</legend>
      <% if(params.errors) { %>
      <div class="control-group">
        <ul>
        <% for(var err in params.errors) { %>
          <li><%= params.errors[err]; %></li>
        <% } %>
        </ul>
      </div>
      <% } %>

      <%- partial('form', {ciudadProfile: params, regiones: regiones}) %>

      <div class="form-actions">
        <%- contentTag('input', 'Add', {type: 'submit', class: 'btn btn-primary'}) %>
      </div>
    </fieldset>
  </form>
</div>
