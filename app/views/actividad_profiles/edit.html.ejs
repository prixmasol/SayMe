<div class="hero-unit">
  <form id="actividadProfile-form" class="form-horizontal" action="/actividad_profiles/<%= params.id %>?_method=PUT" method="POST">
    <fieldset>
      <legend>Editar ActividadProfile</legend>
      <% if(params.errors) { %>
      <div class="control-group">
        <ul>
        <% for(var err in params.errors) { %>
          <li><%= params.errors[err]; %></li>
        <% } %>
        </ul>
      </div>
      <% } %>

      <%- partial('form', {actividadProfile: actividadProfile}) %>

      <div class="form-actions">
        <%- contentTag('button', 'Guardar', {type: 'submit', class: 'btn btn-primary'}) %>
        <%- contentTag('button', 'Borrar', {type: 'submit', formaction: '/actividad_profiles/' + params.id + '?_method=DELETE', formmethod: 'POST', class: 'btn btn-danger'}) %>
      </div>
    </fieldset>
  </form>
</div>
