<div class="hero-unit">
  <form id="profesionalTimeTable-form" class="form-horizontal" action="/profesional_time_tables/<%= params.id %>?_method=PUT" method="POST">
    <fieldset>
      <legend>Update this ProfesionalTimeTable</legend>
      <% if(params.errors) { %>
      <div class="control-group">
        <ul>
        <% for(var err in params.errors) { %>
          <li><%= params.errors[err]; %></li>
        <% } %>
        </ul>
      </div>
      <% } %>

      <%- partial('form', {profesionalTimeTable: profesionalTimeTable}) %>

      <div class="form-actions">
        <%- contentTag('input', 'Save', {type: 'submit', class: 'btn btn-primary'}) %>
        <%- contentTag('button', 'Remove', {type: 'submit', formaction: '/profesional_time_tables/' + params.id + '?_method=DELETE', formmethod: 'POST', class: 'btn btn-danger'}) %>
      </div>
    </fieldset>
  </form>
</div>
