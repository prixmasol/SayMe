<div class="hero-unit">
  <form id="menuProfile-form" class="form-horizontal" action="/menu_profiles" method="POST">
    <fieldset>
      <legend>Create a new MenuProfile</legend>
      <% if(params.errors) { %>
      <div class="control-group">
        <ul>
        <% for(var err in params.errors) { %>
          <li><%= params.errors[err]; %></li>
        <% } %>
        </ul>
      </div>
      <% } %>

      <%- partial('form', {menuProfile: params, pOpcion: pOpcion, pTU: pTU}) %>

      <div class="form-actions">
        <%- contentTag('input', 'Add', {type: 'submit', class: 'btn btn-primary'}) %>
      </div>
    </fieldset>
  </form>
</div>
